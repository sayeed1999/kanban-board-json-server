const backlog = require('./data/backlog');
const blocked = require('./data/blocked');
const colors = require('./data/colors');
const doing = require('./data/doing');
const done = require('./data/done');
const ready = require('./data/ready');
const review = require('./data/review');
const teams = require('./data/teams');
const users = require('./data/users');

module.exports = {
    backlog,
    blocked,
    colors,
    doing,
    done,
    ready,
    review,
    teams, 
    users,
};